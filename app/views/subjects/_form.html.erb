<%= form_for @subject do |f| %>

  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_field :subject, style: "text-transform:uppercase"%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
